package help.sausage.ui.component;

import com.vaadin.flow.component.orderedlayout.VerticalLayout;

public class ReviewListComponent extends VerticalLayout {

//    private DataProvider<Review, ?> dataProvider;
//    private final CriminalClient crimClient;

    public ReviewListComponent() {
//        this.crimClient =
//                ApplicationContextProvider.getCtx().getBean(CriminalClient.class);
//        long maxReviews = crimInfo.total();
//        this.dataProvider = DataProvider.fromCallbacks(query -> {
//            List<ReviewDto> body = crimClient
//                .getReviewForCrim(crimInfo.username(), query.getOffset(), query.getLimit())
//                .getBody();
//            return body == null
//                ? Stream.empty()
//                : body.stream().map(Review::fromDto);
//            }, query -> (int) maxReviews);
//        dataProvider.fetch(new Query<>());
    }

//    @Override
//    public void setDataProvider(DataProvider<Review, ?> dataProvider) {
//        this.dataProvider = dataProvider;
//    }


}
